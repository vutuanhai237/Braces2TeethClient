
const initialState = {
    uploadedImage: null,
    selectedModel: "",
};

function Braces2TeethReducer(state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}

export default Braces2TeethReducer;
